# 安全策略

## 报告安全问题

如果你发现了安全漏洞（尤其是任何可能泄露密码、绕过登录校验、或把本地凭据发出去的问题），**请不要开公开 Issue**，改用：

- 仓库 **Security** 标签页 → **Report a vulnerability**（私密漏洞报告），或
- 直接通过 GitHub 个人资料页上的联系方式私下联系仓库所有者。

确认后会尽快修复，并在 Release 说明里注明。

## 本工具如何处理凭据

- 密码用 **Windows DPAPI**（`CryptProtectData`，附加熵 `OpenSAPGUI/v1`）加密后写入本机 `config.json`，密钥绑定当前 Windows 用户 —— 换电脑或重装系统后解不开，这是设计取舍，不是缺陷；
- 程序**不联网、不上传任何数据**，没有遥测、没有自动更新回传；
- 内存中的明文密码不写日志：`LoginTarget.__repr__` 会把密码显示为 `***`；
- 程序只读写程序同级目录下的 `config.json`（或 `SAP_CONFIG_FILE` / `--config` 指定的路径）和日志文件。

## 使用者需要注意的事

- `config.json` 含连接名、用户名和加密密码，**不要提交到任何版本库、不要连程序一起发给别人**；仓库的 `.gitignore` 已经把 `config.json`、`.env*`、`*.log` 排除，但不要依赖它兜底；
- 用 `--password` 在命令行传密码会暴露在命令行参数里（同机其它进程可见），仅建议临时排障时使用；
- 分发打包时务必**剔除 `config.json`** —— `dist\OpenSAPGUI\` 里那份是你自己的真实配置，发给别人等于把你的连接信息一起给出去了；
- 本工具通过 SAP GUI Scripting 驱动 SAP GUI，请在**你已获得授权**的系统与账号上使用，并遵守所在组织的 IT 与安全规范。

## 支持的版本

只维护 `master` 分支的最新代码和最新 Release。旧版本的问题请先升级到最新版复现。
