# 本地配置：连接名 / 用户名 / DPAPI 加密密码都在里面，仅存本机
config.json
config.json.tmp

# 凭据类文件一律挡住（本程序已不使用 .env 配置，纯属防误提交的安全网）
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# PyInstaller 产物
build/
dist/
*.spec.bak

# 运行日志（含连接名/用户名，不入库）
*.log

# 根目录的历史界面截图（本地验证用）；正式截图在 docs/images/，需入库
/*-preview.png

# IDE
.vscode/settings.json
.workbuddy/
