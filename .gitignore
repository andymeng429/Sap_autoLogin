# 含明文密码，绝不能入库
.env
.env.migrated

# 本地配置：连接名 / 用户名 / DPAPI 加密密码都在里面，仅存本机
config.json
config.json.tmp

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# PyInstaller 产物
build/
dist/
*.spec.bak

# 运行日志（含连接名/用户名，不入库）
*.log

# 界面预览截图（本地验证用）
*-preview.png

# IDE
.vscode/settings.json
.workbuddy/
